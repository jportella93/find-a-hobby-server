#   To create your own env variables:
#   1.Remove the (#)s starting in line 6.
#   2.Change the examples for your own values.
#   3.Rename the file to '.env'.

# PORT=3000
#
# Used to sign the lightweight "client id" token returned in `X-Token`.
# Set this in production so tokens are stable and non-forgeable.
# JWT_SECRET=change-me
#
# MongoDB connection string (use MongoDB Atlas in production)
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
#
# Optional: Redis (for recommendations). If unset, recommendations fall back to random.
# REDIS_URL=redis://default:<password>@<host>:<port>
# REDIS_PORT=6379
# REDIS_AUTH=<password>
